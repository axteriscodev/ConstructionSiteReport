@using ConstructionSiteLibrary.Model
@using Microsoft.AspNetCore.Components.Routing;
@inject NavigationService Navigation

<ScreenComponent MediumWith="641" OnScreenDimensionChanged="ScreenDimensionObserver" />
<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <span class="oi oi-monitor" style="color:white;" aria-hidden="true"></span>
        @if (!@IconMenuActive)
        {
            <a class="navbar-brand" href="" onclick="@(()=>OnLinkClick(PageRouting.HomePage))">Construction Site Report</a>
        }
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
@*         <div class="nav-item px-3">
            <NavLink class="nav-link" style="padding: .5rem .9rem!important;" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Home</label>
                }
            </NavLink>
        </div> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.DocumentCompilationPage">
                <span class="bi bi-check2-square" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Documenti</label>
                }
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.TemplateCreationPage">
                <span class="bi bi-plus-square @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Creazione</label>
                }
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.CategoriesPage">
                <span class="bi bi-bookmarks @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Categorie</label>
                }
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.QuestionPage">
                <span class="bi bi-question-square @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Domande</label>
                }
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.ChoicesPage">
                <span class="bi bi-exclamation-square @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Scelte</label>
                }
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.ConstructorSitePage">
                <span class="bi bi-building-fill @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Cantieri</label>
                }
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.ClientsPage">
                <span class="bi bi-person-lines-fill @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Clienti</label>
                }
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link navItem-size" href="@PageRouting.CompaniesPage">
                <span class="bi bi-shop @biClassRules" aria-hidden="true"></span>
                @if (!@IconMenuActive)
                {
                    <label>Aziende</label>
                }
            </NavLink>
        </div>

    </nav>
</div>

<div class="bottom-row">
    <div class="icon-menu-arrow">

        <RadzenButton Icon=@menuIcon Click="ToggleIconMenu" Style="color: white;" Variant="Variant.Text" />



    </div>
</div>

@code {
    //bool to send to MainLayout for shrinking sidebar and showing/hide menu text
    private bool IconMenuActive { get; set; } = false;

    //EventCallback for sending bool to MainLayout
    [Parameter]
    public EventCallback<bool> ShowIconMenu { get; set; }

    private bool collapseNavMenu = true;

    private string menuIcon = "arrow_back";

    private string biClassRules = "";

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;

    }

    //Method to toggle IconMenuActive bool and send bool via EventCallback
    private async Task ToggleIconMenu()
    {
        IconMenuActive = !IconMenuActive;
        menuIcon = !IconMenuActive ? "arrow_back" : "arrow_forward";
        biClassRules = !IconMenuActive ? "" : "margin-right-zero";
        await ShowIconMenu.InvokeAsync(IconMenuActive);
    }

    private async Task ScreenSizeObserver(ScreenSize size)
    {
        if (size.Width < 641)
        {
            IconMenuActive = false;
            biClassRules = "";
            await ShowIconMenu.InvokeAsync(IconMenuActive);
        }
    }

    private async Task ScreenDimensionObserver(ScreenDimension? dimension)
    {
        if (dimension == ScreenDimension.Small)
        {
            IconMenuActive = false;
            biClassRules = "";
            await ShowIconMenu.InvokeAsync(IconMenuActive);
        }
    }

    private void OnLinkClick(string url)
    {
        Navigation.ChangePage(url);
    }
}
